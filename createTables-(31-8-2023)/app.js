// Create multiplication Tables using for loop

for(let i = 1; i<=20; i++){
    console.log(`multiplication table  ${i}`);

    for(let j=1; j<=10; j++){
        console.log(`${j} * ${i} = ${i * j}`)
}
}